const {StyleSheet} = require('react-native');

const styles = StyleSheet.create({
  accorHeaderIcon: {fontSize: 30, fontWeight: 'bold'},
  container: {
    height: 60,
    width: 120,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'flex-end',
    marginBottom: 5,
  },
  defaultOption: {fontSize: 20, fontWeight: '600'},
});

export default styles;
