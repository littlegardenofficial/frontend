import {THEME_CONTRAST_COLOR} from '../../styles/theme';

const {StyleSheet} = require('react-native');

const styles = StyleSheet.create({
  searchWrapper: {
    width: '100%',
    height: '100%',
    backgroundColor: THEME_CONTRAST_COLOR,
  },
});

export default styles;
